# ASHRAE 62.1 table semantics

Planning placeholder for future sessions.

Prerequisites: provision semantics foundation plus structurally reliable tables.

Goal: add reviewed semantic meaning for source-defined table dimensions, values, qualifiers, units, and provenance where justified by evidence.

Do not treat structural extraction as reviewed semantics. Refine exact scope from measured table coverage before implementation.
