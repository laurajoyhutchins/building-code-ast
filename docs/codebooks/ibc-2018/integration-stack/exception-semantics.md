# IBC 2018 exception semantics

Predecessor: `feature/ibc-applicability-semantics`.

Owns:
- generic Provision AST exception relationships required by IBC;
- inline, referenced, multiple, and nested exception semantics;
- applicability interaction, exact source spans, and explicit unresolved forms;
- bounded IBC fixtures demonstrating reviewed versus parser-derived meaning.

Does not own:
- table lookup semantics;
- calculations;
- amendments, adoption, jurisdiction, or compliance conclusions.

Completion:
- issue #3 exception semantics are documented and schema-backed;
- synthetic fixtures cover multiple and nested exceptions;
- representative IBC exception records project with exact evidence;
- detection alone never implies semantic approval.

Successor: `feature/ibc-table-semantics-contract`.
