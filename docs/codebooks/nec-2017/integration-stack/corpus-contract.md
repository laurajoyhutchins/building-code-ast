# NEC 2017 corpus contract

Predecessor: current `main`; durable publication root is already merged.

Owns:
- defining the exact NEC 2017 whole-publication corpus boundary against the registered retained artifact;
- establishing measurable structural denominators for chapters, articles, parts, sections, parentheticals, definitions, exceptions, notes, tables, figures/graphics, annexes, and references;
- defining source-safe aggregate reporting and explicit unsupported/ambiguous accounting;
- keeping private source bytes and reconstructive output outside Git.

Does not own:
- new PDF extraction behavior;
- hierarchy or non-prose recognition implementation;
- semantic interpretation or Provision AST projection;
- project applicability or compliance evaluation.

Completion:
- every later whole-NEC completeness claim has a reproducible denominator and source-artifact identity;
- unsupported and ambiguous classes are counted rather than omitted;
- corpus measurements are non-reconstructive and publication-safe;
- no protected NEC expression enters Git.

Successor: `feature/nec-2017-full-layout-evidence`.
