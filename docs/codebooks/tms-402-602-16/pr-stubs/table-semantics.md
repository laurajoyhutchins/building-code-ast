# TMS 402-16 reviewed table semantics

Status: draft PR scaffold; implementation not started.

## Purpose

Add reviewed semantic meaning for selected structurally reconstructed TMS 402 tables without turning geometric extraction into automatic lookup logic.

## Scope

- reviewed dimensions, headers, units, notes, and footnotes
- explicit applicability and lookup dimensions
- source-backed lookup behavior for selected tables
- ambiguity/unsupported state
- exact structural-table provenance and review identity

## Boundaries

Do not infer semantics from geometry alone, silently interpolate, generalize beyond reviewed tables, promote commentary tables to normative data, or execute project calculations.

## Completion gate

Selected contrasting TMS 402 tables have reviewed lookup semantics with exact structural provenance, bounded applicability, and explicit ambiguity handling. Remove this scaffold file when implementation replaces it.
