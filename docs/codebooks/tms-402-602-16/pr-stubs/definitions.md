# TMS 402-16 definitions

Status: draft PR scaffold; implementation not started.

## Purpose

Model TMS 402 definition structure and scope without collapsing component context into a global combined-TMS dictionary.

## Scope

- deterministic definition-entry identity
- term and definition source spans
- publication/component and section scope
- repeated/overlapping term handling
- definition-use candidates and explicit unresolved/ambiguous relationships
- source-role preservation and exact-source measurement against the structural inventory

## Boundaries

Do not infer project applicability, import TMS 602 definitions into TMS 402, or treat identical terms as semantically identical without evidence.

## Completion gate

Measured TMS 402 definition structures are deterministically represented with explicit scope and unresolved states. Remove this scaffold file when implementation replaces it.
