# ACI 318-19 integration closeout

Predecessor: `feature/aci-318-19-semantic-measurement`.

Evidence join: `feature/aci-318-19-publication-state-reconciliation` must be considered before claims broader than the exact retained artifact.

Owns:
- final exact-source replay and deterministic compiler receipt;
- calibrated structural, parsed-semantic, and reviewed-semantic capability claims;
- explicit remaining unsupported and ambiguous registries by source role;
- documentation, source-register, LORE, and Deciduous consistency where durable generators support them;
- removal of superseded ACI-only integration paths and final public/private boundary verification.

Does not own:
- new semantic feature families;
- claims that every ACI provision is executable;
- reinforced-concrete design, structural analysis, project responsibility assignment, compliance conclusions, or legal advice;
- silently applying corrections not represented by explicit evidence.

Completion:
- exact retained bytes replay deterministically through the supported compiler stages;
- normative Code and Commentary remain distinct in every exported capability;
- support claims are measured, source-safe, and distinguish generated from reviewed output;
- publication-state uncertainty is stated plainly;
- remaining unsupported classes are documented rather than hidden.

Terminal milestone: ACI 318-19 is fully integrated into Building Code AST under the declared capability contract.
