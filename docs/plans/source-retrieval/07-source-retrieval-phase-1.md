# Source retrieval Phase 1 closeout

Status: scope-only coordination PR stub.

Purpose: consolidate the first usable local retrieval system after both lexical branches are implemented.

Primary ancestry: `feature/source-search-cli`.
Required sibling before implementation/landing: `feature/source-structural-search`.

Owns only integration reconciliation, CLI exposure of structural filters, local workflow documentation, one bounded real-source verification, privacy checks, and performance measurements.

Excludes new retrieval architecture, embeddings, publication semantics, and AST interpretation.

Completion gate: Phase 1 is usable for real AST investigation and its value/limitations are measured before semantic search begins.

Successor after the value gate: `feature/source-semantic-index`.
