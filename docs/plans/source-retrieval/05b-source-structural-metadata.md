# Source structural metadata

Status: scope-only stacked PR stub.

Purpose: add publication-neutral source-observation features useful for investigation without assigning code semantics.

Owns optional font/layout/block measurements and explicitly derived candidates such as heading/table/figure/equation candidates.

Excludes semantic labels such as requirement, exception, definition, commentary, mandatory, or informative unless supplied by publication-specific parsers.

Completion: evidence can be inspected and filtered by useful layout characteristics while observed and derived fields remain distinct.

Predecessor: `feature/source-lexical-search`.
Successor: `feature/source-structural-search`.
