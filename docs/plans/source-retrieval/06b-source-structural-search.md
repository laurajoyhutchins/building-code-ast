# Source structural search

Status: scope-only stacked PR stub.

Purpose: compose lexical retrieval with publication-neutral source-observation constraints.

Owns typed filtering by publication, page ranges, observed layout fields, and derived structural candidates.

Excludes publication semantics and semantic/vector ranking.

Completion: source characterization can ask bounded questions such as finding matching labels with non-body typography without encoding what those labels mean in the shared layer.

Predecessor: `feature/source-structural-metadata`.
Sibling dependency before Phase 1 closeout: `feature/source-search-cli`.
